name: Foxy RHEL Binary Build
on:
  workflow_dispatch:
    branches:
      - foxy
  pull_request:
    branches:
      - foxy
  push:
    branches:
      - foxy
  schedule:
    # Run every day to detect flakiness and broken dependencies
    - cron: '03 1 * * *'


jobs:
  foxy_rhel_binary:
    name: Foxy RHEL binary build
    runs-on: ubuntu-latest
    env:
      ROS_DISTRO: foxy
    container: ghcr.io/ros-controls/ros:foxy-rhel
    steps:
      - uses: actions/checkout@v3
        with:
          path: src/kuka_experimental
      - run: |
          rosdep update
          rosdep install -iy --from-path src/kuka_experimental
          source /opt/ros/${{ env.ROS_DISTRO }}/setup.bash
          colcon build
          colcon test
